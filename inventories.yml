all:
  children:
    haproxy:
      hosts:
        haproxy-0:
          ansible_host: 10.255.0.112
    kubernetes:
      children:
        k8s_masters:
          hosts:
            kube-master-0:
              ansible_host: 10.0.0.20
        k8s_workers:
          hosts:
            kube-worker-0:
              ansible_host: 10.0.0.30
jump:
  hosts:
    kube-worker-0:
    kube-master-0:
